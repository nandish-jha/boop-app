<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <queries>
        <intent>
            <action android:name="android.app.action.REQUEST_ROLE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Boop">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <receiver
            android:name=".BoopTasksWidget"
            android:exported="true"
            android:label="@string/widget_tasks_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/boop_tasks_widget_info" />
        </receiver>

        <receiver
            android:name=".BoopNotesWidget"
            android:exported="true"
            android:label="@string/widget_notes_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/boop_notes_widget_info" />
        </receiver>

        <receiver
            android:name=".BoopHabitsWidget"
            android:exported="true"
            android:label="@string/widget_habits_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/boop_habits_widget_info" />
        </receiver>

        <receiver
            android:name=".BoopUpNextWidget"
            android:exported="true"
            android:label="@string/widget_up_next_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/boop_up_next_widget_info" />
        </receiver>

        <receiver
            android:name=".BoopWalletWidget"
            android:exported="true"
            android:label="@string/widget_wallet_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/boop_wallet_widget_info" />
        </receiver>

        <receiver
            android:name=".BoopOverdueWidget"
            android:exported="true"
            android:label="@string/widget_overdue_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/boop_overdue_widget_info" />
        </receiver>

        <receiver
            android:name=".BoopStreakWidget"
            android:exported="true"
            android:label="@string/widget_streak_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/boop_streak_widget_info" />
        </receiver>

        <receiver
            android:name=".BoopTodaySummaryWidget"
            android:exported="true"
            android:label="@string/widget_today_summary_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/boop_today_summary_widget_info" />
        </receiver>

        <receiver
            android:name=".TaskReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".HabitReminderReceiver"
            android:exported="false" />
    </application>
</manifest>
